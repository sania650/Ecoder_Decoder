# Ecoder_Decoder
app that lets a user to do Caesar Cipher. The Caesar Cipher works by replacing each letter in a message with a letter a fixed number of positions down the alphabet. This fixed number is the Key. If the key is 1, an "A" becomes a "B", "B" becomes "C", etc. If the key is 3, "HELLO" becomes "KHOOR". 
